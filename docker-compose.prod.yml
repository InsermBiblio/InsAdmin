server:
    image: vsnexus.intra.inist.fr:8083/bibcnrs/bibadmin:${bibadmin_version}
    environment:
        NODE_ENV: production
    working_dir: /public
    restart: always
    ports:
        - 3010:8080
